server:
  port: 9000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8900/eureka/
spring:
  application:
    name: gateway-myblog
zuul:
  routes:
    api-a:
      path: /app-user/**
      serviceId: user-consumer
    api-b:
      path: /app-sadfasefd/**
      serviceId: feign-consumer